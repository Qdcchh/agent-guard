# 开发代理约束

适用于本仓库中的 AI 辅助开发和自动化修改。当前项目处于骨架阶段，不能将设计文档描述为已实现能力。

## 工作顺序

1. 阅读 README、`docs/protocol-v1.md`、`docs/execution-state.md` 及相关模块测试。
2. 检查工作区现有修改，不覆盖、不回退其他成员的工作。
3. 明确目标、验收标准、影响范围及未决协议问题，再实施最小必要修改。
4. 同步契约、实现、调用方和测试；运行检查，报告实际结果及未执行项目。
5. 没有用户明确授权时不 commit、push、创建远程仓库或 PR；不修改 Git 用户配置，不跳过 hooks，不强推。

## 安全不变量

- 不自研或伪造 SM2/SM3；选用成熟库并验证测试向量及互操作。
- 统一规范编码和域分离。不得在不同模块中自行拼接签名字符串。
- 拒绝重复字段、未知安全字段、歧义类型、负数、浮点金额和溢出；布尔值不得被视为整数。
- v1 金额为 CNY 整数分。权限必须逐级收窄，包含租户、任务、audience、工具、资源、参数、金额、次数、有效期与深度。
- 根及全部适用祖先共享累计额度；接受事务内检查撤销、有效期和额度，不能仅信任先前验权结果。
- 业务幂等键不等于 nonce。所有外部重试及结果读取仍须当前验权与新鲜性验证。
- 下游超时表示结果可能未知，不得据此释放预算。恢复仅处理已接受且不可变的意图。
- 模型不得持有绕过网关的凭据。不得添加运行环境可开启的跳过验权开关；测试替身仅限测试装配。
- 通知仅允许固定模板与批准接收方，不开放任意 URL、脚本或任意消息转发。
- 不将本地哈希链称为独立审计。明确检查点的独立保存与未锚定尾部局限。

## 工程规则

- 只提交合成数据；不提交私钥、API token、`.env`、数据库数据、原始敏感日志。
- 本地开发密钥应由显式初始化流程生成。固定测试密钥必须醒目标识且不能用于部署。
- 安全测试脱离大模型可独立执行。功能实现必须覆盖拒绝、并发及恢复路径。
- 数据库变更使用版本迁移，文档说明升级方式及不兼容风险。
- 引入依赖前说明目的、版本、维护状况与许可；引入运行依赖时建立可复现锁文件。
- 协议变更更新版本及兼容策略；不悄悄改变签名语义或工具操作含义。
- 不因时间压力删除预算、撤销、幂等和证据检查。应优先削减可选场景和页面。

## 验证及汇报

```bash
python -m ruff check .
python -m ruff format --check .
python -m pytest
```

增加模块后补充相关集成测试。最终回复列出改动、验证命令、实际结果及未解决问题。性能和安全结论必须附条件，不声称绝对安全、生产级或未经证实的原创性。文件写入后必须读回或检查 diff，不以子代理口头成功作为证据。
